//define a new module. The first arugment which is what we want to call our app. the second is an array of depenances which we dont ened at the moment, so there are none
angular.module('artApp',[]); //init new angular module, name string then empty array
